[workspace]
members = ["geozero", "geozero-core", "geozero-cli", "geozero-bench"]
default-members = ["geozero", "geozero-core", "geozero-cli"]

[patch.crates-io]
# Patch flatgeobuf dependency (dev only)
geozero = { path = "geozero" }
